#include "structure.h"

	Structure::Structure(std::string n, int l, int a, int p, Resource *){
							name = n;
							level = l;
							amount = a;
							prereq	= p;
							cost = c;
							
	}

Structure::Structure(){ }

